# OCLP7 checkpoint — permanent FASTLANE hardware-feedback contract / D97AEZ minimal GitHub build

Date: 2026-09-02 EEST

## Authoritative user correction
The established project method must not be replaced by either extreme: the user must not be made to combine or compile work that can run in GitHub, and GitHub must not be treated as a substitute for the ASUS2 hardware test.

Historically the valid accelerated diagnostic cycle was:

1. the assistant produced and validated a FASTLANE that integrated the diagnostic into OCLP;
2. the user ran one complete deploy action;
3. after assistant audit, the user manually Root Patched when the new OCLP build required it;
4. after Root Patch audit, the user booted accelerated;
5. because the machine produced no usable image, the user hard-restarted/power-cycled and entered VESA;
6. the assistant selected and analyzed only the preceding accelerated boot and then defined the next action.

This feedback loop remains authoritative. The GitHub lane must now carry every test of the FASTLANE implementation that does not inherently require ASUS2. The ASUS2 lane contains only the minimal real-machine action and its returned evidence.

## Permanent division of responsibility
Before the user receives a hardware command, the assistant must execute and audit in GitHub, as applicable:

- source/workflow integration and exact diff;
- syntax, compile/link and package construction;
- packaged executable/app and artifact re-extraction audit;
- SHA/blob/bytes/mode/provenance manifests;
- positive, negative, adversarial and contradictory RC fixtures;
- same-boot, next-boot, one-shot, interrupted/incomplete and no-rerun state fixtures;
- report construction, sealing, parsing and retrieval fixtures;
- wrapper identity and supply-chain tests;
- complete workflow logs, job result and artifact identities.

The user must not compile, combine fragments, repair GitHub failures or run repeated exploratory commands that the GitHub lane can simulate. Each real-machine gate is one identity-pinned audited wrapper or one explicitly described manual operation.

GitHub PASS is implementation evidence only. The decisive MTLCompilerService runtime result can come only from ASUS2 during the actual accelerated boot.

## Current D97AEZ application of the rule
The unresolved causal question remains:

**During a new accelerated boot, do naturally spawned exact-path MTLCompilerService processes map the current D97AD 31-window postimage, or different runtime text?**

D97AD is already installed with exact stable visible service/target identities. No new functional patch and no new Root Patch are presently justified. D97AEZ must therefore be observation transport, not another compiler patch.

The smallest acceptable D97AEZ FASTLANE is:

1. GitHub builds/tests/packages/audits a passive one-shot boot observer around the unchanged corrected D97AEX universal/no-PID helper;
2. only after complete PASS, the user runs one pinned activation/deployment wrapper in VESA;
3. assistant audits that complete activation report;
4. user manually enters the already-root-patched accelerated configuration;
5. observer overlaps the naturally spawned cohort without launching or stopping MTLCompilerService;
6. user waits the stated bounded interval, manually hard-restarts/power-cycles, and enters VESA;
7. user runs one pinned retrieval wrapper;
8. assistant binds the report to the exact accelerated boot and applies the causal decision table.

A foreground observer started after VESA recovery is not an accelerated test. The observer must already be active for the preceding accelerated cohort.

## Causal decision boundary retained

- all 31 D97AD windows MATCH on a completely captured observed accelerated cohort: only the known P7-vs-D97AD bounded-provenance mismatch hypothesis becomes NEGATIVE within those tested windows; no whole-image or runtime-semantic identity claim;
- one or more bounded-window MISMATCH results: runtime image selection/deployment/cache provenance becomes the active frontier;
- no natural process, task-read denial, UUID negative, race, wrong boot, interrupted report or incomplete coverage: INCOMPLETE/UNKNOWN, with no functional patch inference.

## Safety and feedback gates
D97AEZ must never launch/stop MTLCompilerService, mutate target/Golden bytes, open OCLP, auto Root Patch or auto reboot. Any observer-owned system mutation must be explicit, identity-pinned, bounded and removable.

Work proceeds through visible gates. The assistant reports each completed GitHub milestone and does not cross into ASUS2 deployment/boot without exact evidence and explicit authorization. A failed GitHub or ASUS2 gate causes STOP and repair in the proper lane, not scattered user-side fixes.

## CURRENT ACTION
Assistant: complete the minimal D97AEZ GitHub implementation, state/RC/adversarial tests, packaging and full artifact audit. User: STOP. No command, OCLP opening, Root Patch or reboot is authorized now.

Baseline remains exactly P1+P2b+P3+AIR00+D34. Golden is immutable/read-only. D50/D68/D82 remain reserve-only; D84 retired; Patch8 unauthorized.
