Continuăm proiectul ASUS2 Tahoe Intel x86_64 Haswell / OCLP din starea autoritativă persistentă din repo `StefanAlMare/StefanAlMare`.

Înainte să propui ORICE modificare, citește INTEGRAL, în această ordine:
1. `OCLP-Continuity/OCLP_PERMANENT_WORKING_RULES.md`
2. `OCLP-Continuity/OCLP_MASTER_CONTINUITY.md`
3. `OCLP-Continuity/OCLP_PERMANENT_VESA_RECOVERY_RULE.md`
4. checkpoint-ul exact indicat de MASTER la `Current authoritative checkpoint`
5. `OCLP-Continuity/OCLP_PROJECT_RETROSPECTIVE_20260827.md`
6. `OCLP-Continuity/OCLP_HISTORY_INDEX.md`

Aceste fișiere sunt sursa de adevăr; nu reconstrui current state din memorie și nu mă pune să repet istoria. Reia exact de la `CURRENT ACTION`.

Starea reconciliată la 2026-09-08 este P1+P3-only, nu true-five activ:
- P1 exact este activ/proven;
- P3 exact P3-only este activ/proven;
- P2 rămâne original, fără P2b;
- AIR00 și D34 nu sunt active;
- corrected metallibs sunt 180/180 exact;
- GUI este încă NEGATIVE/no usable GUI;
- P1 runtime semantic progress este PROVEN;
- P3 runtime semantic progress este PROVEN;
- P2b este doar candidat plauzibil și `NOT_YET_AUTHORIZED`.

Experimentul accelerat P1+P3 relevant se identifică permanent prin fereastra:
`2026-09-08 14:24:02 -> aproximativ 14:27:17 +0300`.
Nu folosi formulări persistente de tip penultimul/antepenultimul boot; ele se schimbă după rebooturi ulterioare. User-ul identifică autoritativ care boot a fost accelerat și care a fost VESA recovery.

D97HV are un defect de tooling: parserul `kern.boottime` a produs 1970 și a contaminat automat clasificarea cu IPS istorice. `P3_MIXED_CURRENT_BOOT_FRONTIER` din outputul automat D97HV este INVALID. Raw evidence este util numai după re-scoping la fereastra accelerată de mai sus.

Fereastra accelerată P1+P3 a arătat:
- zero current old NULL-call MTLCompilerService crash;
- zero current P1-only StringMap SIGSEGV;
- 145 MTLCompilerService invocations în noul simulator/bitcode diagnostic path;
- compiler connection loss -> XPC_ERROR_CONNECTION_INTERRUPTED -> GPUPass/render-pipeline failure -> WindowServer restart -> no GUI.
Nu identifica încă exact validatorul drept `MTLSimCompiler::validSimulatorMetadata` până când static map-ul curent nu dovedește asta.

Modul permanent de execuție este GitHub-first:
- tot ce este tehnic executabil în GitHub îl execută și îl auditează asistentul acolo;
- ASUS2/user este rezervat acțiunilor identity-pinned care cer hardware/live installed state;
- compilarea locală nu este fallback implicit și cere autorizare explicită;
- niciodată auto Root Patch sau auto reboot;
- Golden Sequoia rămâne immutable/read-only.

Păstrează FASTLANE:
`validări -> integrare -> compile/diff -> build -> audit app -> SHA/identitate -> backup/deploy -> open OCLP -> STOP`.

CURRENT ACTION este D97HW:
`OCLP-Continuity/artifacts/OCLP7_D97HW_READONLY_P3_SIMULATOR_BITCODE_STATIC_MAP.sh`.
D97HW NU a fost încă executat și nu există rezultat D97HW.

Scop D97HW:
- pin exact P1/P3 active;
- confirmă P2 original și P3 exact;
- recuperează full strings `simulator`/`bitcode`;
- mapează offsets/VA/xrefs/disassembly;
- leagă calea de P2 `0x9A8CD` și P3 `0xA1573`;
- inspectează `MTLSimCompiler::validSimulatorMetadata` numai dacă binary evidence curent îl susține;
- decide dacă P2b este upstream/causal.

D97HW este read-only: fără Root Patch, Restore, EFI/NVRAM/framebuffer mutation, acceleration change sau reboot.

Persistă imediat orice rezultat decisiv în MASTER + HISTORY + DATABASE (dacă schimbă durable current state) + checkpoint nou.